* Extract test cases from original projects this was extracted from.
* Add API documentation.
* Add navigation button images.
* Test UIPageControl in app that uses the page indicators. (Previously used a iOS 5 compatible page control that allowed tint color.)
* Automatically disable shadow if pageMargin is 0.
* There is a bug with -[FTWebViewController isAtLastPage] where it returns NO when it actually is at the last page. (See report app.)
